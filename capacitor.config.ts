import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.rv',
  appName: 'ng-vsoft',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
